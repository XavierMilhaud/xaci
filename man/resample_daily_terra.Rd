% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component_terra.R
\name{resample_daily_terra}
\alias{resample_daily_terra}
\title{Resample a SpatRaster to daily resolution (terra version)}
\usage{
resample_daily_terra(r, fun = "mean", filename = "")
}
\arguments{
\item{r}{A \code{terra::SpatRaster} with \code{terra::time()} set
(sub-daily time steps expected).}

\item{fun}{Aggregation function name understood by \code{terra::tapp()}
(e.g. \code{"mean"}, \code{"sum"}, \code{"max"}, \code{"min"}).}

\item{filename}{Optional path to write the result directly to disk (highly
recommended for large jobs). Default \code{""} (terra decides, using a
temp file if the result doesn't fit in memory).}
}
\value{
A \code{terra::SpatRaster} with one layer per day.
}
\description{
Equivalent of \code{resample_daily()}. Groups layers by calendar day and
applies \code{fun}, writing the result to disk chunk-by-chunk via
\code{terra::tapp()} rather than holding the full input in RAM.
}
