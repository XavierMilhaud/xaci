% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wind_terra.R
\name{calculate_period_wind_exceedance_frequency_terra}
\alias{calculate_period_wind_exceedance_frequency_terra}
\title{Calculate monthly wind exceedance frequency (terra version)}
\usage{
calculate_period_wind_exceedance_frequency_terra(
  u10_r,
  v10_r,
  reference_period,
  mask_path = NULL,
  threshold = 0.8,
  target_chunk_gb = 1
)
}
\arguments{
\item{u10_r, v10_r}{\code{terra::SpatRaster}, hourly resolution, non masque.}

\item{reference_period}{Character vector \code{c("start", "end")}.}

\item{mask_path}{Path to the mask NetCDF file, or \code{NULL} (no masking).}

\item{threshold}{Numeric threshold for the mask. Default \code{0.8}.}

\item{target_chunk_gb}{Passed to \code{resample_daily_terra()} (see its
memory note) for memory-safe temporal chunking of large hourly series.}
}
\value{
Same as \code{calculate_period_wind_exceedance_frequency()}.
}
\description{
Drop-in, memory-safe replacement for
\code{calculate_period_wind_exceedance_frequency()}. Only the initial
\code{wind_power_terra()} call differs from the base-R version; the
threshold, crossing, and monthly-aggregation steps reuse the exact same
helpers, since they already operate on small, daily-resolution data.
}
