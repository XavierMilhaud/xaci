% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{standardize_metric}
\alias{standardize_metric}
\title{Standardise a monthly metric relative to a reference period}
\usage{
standardize_metric(metric, reference_period, area = FALSE)
}
\arguments{
\item{metric}{A list with fields \code{data} [lon x lat x time] or [time],
\code{time}, and optionally \code{lon}/\code{lat}.}

\item{reference_period}{Character vector of length 2: start and end dates
(\code{"YYYY-MM-DD"}).}

\item{area}{Logical. If \code{TRUE} the spatial mean is computed before
standardising and a named numeric vector is returned. Default \code{FALSE}.}
}
\value{
If \code{area = TRUE}: a named numeric vector (names = dates).
  Otherwise: a list with the same structure as \code{metric} but standardised
  values.
}
\description{
For each calendar month, computes the mean and standard deviation over the
reference period, then returns (x - mean) / sd. Optionally averages over
the spatial dimensions first.
}
