% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component_terra.R
\name{temp_extremum_terra}
\alias{temp_extremum_terra}
\title{Compute daily temperature extremum (min or max) for day or night hours
(terra version)}
\usage{
temp_extremum_terra(r, extremum, period, filename = "", target_chunk_gb = 1)
}
\arguments{
\item{r}{A \code{terra::SpatRaster}, hourly resolution, with
\code{terra::time()} set.}

\item{extremum}{\code{"min"} or \code{"max"}.}

\item{period}{\code{"day"} (hours 6-21) or \code{"night"} (hours 0-5 and
22-23).}

\item{filename}{Optional output path (see \code{resample_daily_terra()}).}

\item{target_chunk_gb}{Passed to \code{resample_daily_terra()} (see its
memory note) for memory-safe temporal chunking of large hourly series.}
}
\value{
A \code{terra::SpatRaster} with one layer per day.
}
\description{
Equivalent of \code{temp_extremum()}. Filters layers by hour-of-day (a
cheap, lazy operation on a SpatRaster -- no data is read), then reduces to
daily resolution via \code{resample_daily_terra()}.
}
