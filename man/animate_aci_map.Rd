% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{animate_aci_map}
\alias{animate_aci_map}
\title{Animate the ACI or a component over time}
\usage{
animate_aci_map(
  data,
  variable = "ACI",
  country_abbrev = NULL,
  admin_level = NULL,
  crs_metric = 4326,
  var_label = "Standardised anomaly",
  title = "ACI over time",
  palette = "RdBu",
  reverse = TRUE,
  n_breaks = 9,
  borders = TRUE,
  fps = 4,
  width = 800,
  height = 600,
  save_path = NULL
)
}
\arguments{
\item{data}{Grid-cell list or admin \code{data.frame}.}

\item{variable}{Variable to animate. Default \code{"ACI"}.}

\item{country_abbrev}{Three-letter ISO code.}

\item{admin_level}{Integer or \code{NULL}.}

\item{crs_metric}{EPSG code, used for admin choropleth projection only.
Default \code{4326}. Ignored (with a warning if explicitly set to
something else) in raster (grid-cell) mode, where the basemap is always
reprojected to EPSG:4326 to match the fixed lon/lat raster grid.}

\item{var_label}{Colour-bar label. Default \code{"Standardised anomaly"}.}

\item{title}{Plot title. Default \code{"ACI over time"}.}

\item{palette}{RColorBrewer palette. Default \code{"RdBu"}.}

\item{reverse}{Logical. Default \code{TRUE}.}

\item{n_breaks}{Integer. Default \code{9}.}

\item{borders}{Logical. Default \code{TRUE}.}

\item{fps}{Frames per second. Default \code{4}.}

\item{width}{Output width in pixels. Default \code{800}.}

\item{height}{Output height in pixels. Default \code{600}.}

\item{save_path}{File path for the output GIF. If \code{NULL} (default),
the animation is rendered to the viewer but not saved.}
}
\value{
A \pkg{gganimate} animation object (invisibly).
}
\description{
Produces a GIF animation (via \pkg{gganimate}) showing the evolution of
a spatial variable at each time step. Requires \pkg{gganimate} and
\pkg{gifski} (or \pkg{png}) to be installed.
}
\examples{
\dontrun{
grid <- calculate_aci(..., area = FALSE)
animate_aci_map(grid, variable = "ACI", country_abbrev = "FRA",
                fps = 2, save_path = "aci_animation.gif")
}

}
