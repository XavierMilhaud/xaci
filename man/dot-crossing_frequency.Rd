% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temperature.R
\name{.crossing_frequency}
\alias{.crossing_frequency}
\title{Compute monthly threshold-crossing frequency from daily extrema}
\usage{
.crossing_frequency(daily_ext, thresholds_day, above_thresholds, lon, lat)
}
\arguments{
\item{daily_ext}{List with \code{data} [lon x lat x days] and daily
\code{time}, as returned by \code{temp_extremum()}/\code{temp_extremum_terra()}
(converted to list form).}

\item{thresholds_day}{Array \code{[lon x lat x 366]}, as returned by
\code{calculate_percentiles()}/\code{calculate_percentiles_terra()}
(converted to array form).}

\item{above_thresholds}{Logical. \code{TRUE} counts days above the
threshold (hot extremes); \code{FALSE} counts days below (cold
extremes).}

\item{lon, lat}{Coordinate vectors, carried through to the output.}
}
\value{
A list with \code{data} [lon x lat x months] and monthly
  \code{time}.
}
\description{
Shared helper behind \code{calculate_halfday_component()} (base-R) and
\code{calculate_halfday_component_terra()} (terra). Kept independent of
how \code{daily_ext}/\code{thresholds_day} were produced, so both loading
paths reuse the exact same, already-tested logic.
}
\keyword{internal}
