% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temperature_terra.R
\name{calculate_halfday_component_terra}
\alias{calculate_halfday_component_terra}
\title{Calculate the half-day (day or night) temperature component (terra version)}
\usage{
calculate_halfday_component_terra(
  r,
  reference_period,
  part_of_day,
  extremum,
  percentile,
  above_thresholds
)
}
\arguments{
\item{r}{A \code{terra::SpatRaster}, hourly resolution, temperature
already in the desired unit (e.g. Celsius), with \code{terra::time()} set.}

\item{reference_period}{Character vector \code{c("YYYY-MM-DD", "YYYY-MM-DD")}.}

\item{part_of_day}{\code{"day"} or \code{"night"}.}

\item{extremum}{\code{"min"} or \code{"max"}.}

\item{percentile}{Numeric percentile (e.g. 90 or 10).}

\item{above_thresholds}{Logical. \code{TRUE} counts days above the threshold
(hot extremes); \code{FALSE} counts days below (cold extremes).}
}
\value{
Same structure as \code{calculate_halfday_component()}:
  \code{list(data, time, lon, lat)}.
}
\description{
Calculate the half-day (day or night) temperature component (terra version)
}
